# Optional: use Supabase for cloud database and auth.
# Leave empty to use local-only storage (IndexedDB).
# Get these from https://supabase.com/dashboard → your project → Settings → API.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
